# Copy to .env and fill in values. Never commit .env.

HOST=127.0.0.1
PORT=8000
DATA_DIR=data
MAX_UPLOAD_MB=50

# Groq — powers both ASR (Whisper) and summarization (Llama).
# Free tier, no credit card: https://console.groq.com
GROQ_API_KEY=gsk_...
